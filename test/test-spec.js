/* eslint-disable no-unused-expressions*/
/* global describe it */
import { expect } from 'chai';

describe('placeholder tests...', () => {
  it('passes all tests!', () => {
    expect(1).to.equal(1);
  });
});
